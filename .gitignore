.env
.env.local
.env.*.local
node_modules/
__pycache__/
*.pyc
.venv/
venv/
dist/
build/
*.log
.DS_Store

# Local DB backups (ops/backup-db.sh)
backups/

# Local Claude skills/tooling (not part of the app)
.claude/

# Production env (secrets — never commit)
.env.production
.env.production.local

# Raw marketing video sources (served via Cloudflare Stream, not git)
*.mp4
